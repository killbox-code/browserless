name: Webkit

on: [pull_request]

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build the base layer
        run: docker build -f ./docker/base/Dockerfile -t ghcr.io/killbox-code/base:latest .

      - name: Build the Webkit image
        run: docker build --build-arg VERSION=latest -f ./docker/webkit/Dockerfile -t ghcr.io/killbox-code/webkit:latest .

      - name: Test the Webkit image
        run: docker run --ipc=host -e CI=true --entrypoint ./scripts/test.sh ghcr.io/killbox-code/webkit:latest
